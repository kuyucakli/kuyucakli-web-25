<section>
  <div class="plate">
    <div class="side front flex-col-center">
      <h1 class="displaylarge big-hi">Hi!</h1>
    </div>
    <div class="side back"></div>
    <div class="side left"></div>
    <div class="side right flex-col-center" style="color:transparent">
      <p class="bodymedium">
        <span class="displaysmall">I'm</span> Burak Kuyucaklı from Istanbul, a creative
        frontend developer who enjoys making things work smoothly for people through
        code.
      </p>
      <p class="m-v-sm-1 bodymedium">
        While frontend is my main focus, I like exploring the backend too.
      </p>
      <p class="bodymedium">
        Colors, drawing, and user experience keep the process creative and fun.
      </p>
    </div>
    <div class="side top"></div>
    <div class="side bottom"></div>
  </div>
</section>

<style>
  .big-hi {
    font-size: clamp(64px, 50vmin, 200px);
    color: white;
  }
  section {
    height: 100dvh;
    display: flex;
    justify-content: center;
    align-items: center;
    perspective: 1200px;
    perspective-origin: center;
    animation: po 8s infinite alternate;
    --w: clamp(340px, 50vmin, 600px);
    --m: calc(var(--w) / 2);
    --m-negative: calc(-1 * var(--w) / 2);
  }

  .plate {
    width: var(--w);
    aspect-ratio: 1;
    border-radius: 2px;
    position: relative;
    animation: cube 3s 4s forwards linear;
    transform-style: preserve-3d;
  }

  .side {
    position: absolute;
    width: 100%;
    height: 100%;
    opacity: 0;

    border-radius: 4px;

    padding: 32px;
  }

  /* Background colors for the cube's sides to help visualize the work */
  .front {
    background-color: transparent;
    transform: translateZ(var(--m));
    padding: 48px;
    border-radius: 50%;
    animation: front 3s forwards ease-out;
  }
  .back {
    background: #ffec70;
    background: radial-gradient(
      circle,
      rgba(251, 218, 2, 0.53) 9%,
      rgba(255, 221, 0, 1) 153%
    );
    transform: translateZ(var(--m-negative));
    animation: sides 0.2s 2.8s forwards;
  }

  .left {
    background-color: #feed00;
    transform: rotateY(90deg) translateZ(var(--m));
    animation: sides 0.2s 3.2s forwards;
  }
  .right {
    background-color: #ffea00;
    transform: rotateY(-90deg) translateZ(var(--m));
    animation:
      sides 0.2s 4s forwards,
      right-side 0.2s 6s forwards;
  }

  .top {
    background-color: #f7ff02;
    transform: rotateX(90deg) translateZ(var(--m));
    animation: sides 0.2s 2.6s forwards;
  }
  .bottom {
    background-color: #ffdf10;
    transform: rotateX(-90deg) translateZ(var(--m));
    animation: sides 0.2s 2.9s forwards;
  }

  @keyframes sides {
    to {
      opacity: 1;
    }
  }

  @keyframes front {
    0% {
      transform: translateX(0%) translateZ(0) scale(0);
      background-color: white;
      opacity: 1;
      filter: blur(2px);
    }
    5% {
      transform: translateX(5%) translateZ(var(--m)) scale(0.03);
    }
    10% {
      transform: translateX(5%) translateZ(var(--m)) scale(0.04);
    }
    20% {
      transform: translateX(0%) translateZ(var(--m)) scale(0.04);
      border-radius: 20%;
    }
    30% {
      transform: translateZ(var(--m)) scale(0.1);
      background-color: rgb(255, 255, 255);
      border-radius: 5%;
    }
    65% {
      transform: translateZ(var(--m)) scale(0.9);
      border-radius: 2%;
      filter: blur(0);
    }
    70% {
      opacity: 1;
      transform: translateZ(var(--m)) scale(1);
      border-radius: 0;
    }
    80% {
      opacity: 1;
      transform: translateZ(var(--m)) scale(1);
      border-radius: 0;
    }
    85% {
      opacity: 1;
      transform: translateZ(var(--m)) scale(1.1);
      border-radius: 0;
      filter: blur(2px);
    }
    100% {
      opacity: 1;
      transform: translateZ(var(--m)) scale(1);
      border-radius: 2px;
      filter: blur(0);
    }
  }

  @keyframes right-side {
    from {
      background-color: #eaeaea;
      color: black;
    }
    to {
      background: #9c7f1e;
      background: radial-gradient(
        circle,
        rgb(195, 159, 66) 20%,
        rgb(255, 230, 64) 100%
      );
      color: white;
      border-color: transparent;
    }
  }

  @keyframes cube {
    0% {
      transform: rotateY(0deg);
    }
    10% {
      transform: rotateY(20deg);
    }
    20% {
      transform: rotateY(0deg);
    }
    30% {
      transform: rotateY(40deg);
    }
    40% {
      transform: rotateY(120deg);
    }
    50% {
      transform: rotateY(360deg);
    }

    100% {
      transform: rotateY(90deg);
    }
  }

  @keyframes po {
    0% {
      perspective-origin: left;
    }

    100% {
      perspective-origin: right;
    }
  }
</style>
